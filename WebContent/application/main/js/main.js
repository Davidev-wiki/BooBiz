/**
 * BooBiz 메인 페이지 스크립트 
 */

var lid = "";

$(document).ready(function(){
	// pop up open
	//window.open("javascript 실시간 동작중..");
	
	$(".list").click(function(){
		
		lid = $(This).getElimentedById();
		switch (lid) {
		
		// jsp include 의 경우, 페이지가 고정되어 박혀있을 것,
		// 클릭 이벤트로 다른 페이지를 불러오는 방법..?
		// 아이디를 
		// css.display attribute?
		// 
		
		
		}
		
		
		
	});
	
});
